<% if @additional_info.errors.any? %>
  <div id="error_explanation">
      <h2>
        <%= pluralize(@additional_info.errors.count, "error") %> prohibited this additional info from being saved:
      </h2>
      <ul>
        <% @additional_info.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
        <% end %>
      </ul>
    </div>

<% end %>

<%=debug @additional_info%>
